// version.js - single source of truth for the app's user-facing version tag,
// shown next to the title in App.jsx. Bump this on every user-facing change
// (UI, ingest logic, schema). See CLAUDE.md "Versioning" for the convention.
export const APP_VERSION = 'v1.6.0';
